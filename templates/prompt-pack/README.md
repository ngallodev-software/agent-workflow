# Prompt-pack templates

Use `agent-workflow pack scaffold` rather than copying individual files manually. These templates encode the durable archive format: phase decomposition, bounded tickets, fresh terminal sessions, narrow testing, source references, completion reports, review gates, and checksums.
