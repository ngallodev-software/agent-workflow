# Delegation runbook

Use the repository `docs/references/DELEGATION_RUNBOOK.md`. Implementation is
interactive by default, runs in an isolated worktree, and requires independent
review before backlog acceptance.
