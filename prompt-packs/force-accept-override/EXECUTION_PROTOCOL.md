# Execution protocol

Use the repository `docs/references/EXECUTION_PROTOCOL.md` as the canonical
protocol. Preserve receipt authority, run exact-worktree preflight before code
discovery, and do not claim user-only authentication that the current runtime
cannot enforce.
