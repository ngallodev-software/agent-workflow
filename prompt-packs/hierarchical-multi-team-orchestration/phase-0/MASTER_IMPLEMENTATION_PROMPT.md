# Phase 0 — independently review hierarchy authority

Treat HIER-001 and HIER-002 as implemented and in review. Run HIER-GATE-0 over the dedicated `agent_workflow.hierarchy` package: fixed-depth immutable contracts, capability/budget narrowing, append-only local journals, idempotent imports, deterministic replay, and digest-sealed read-only team/root receipts. Verify that receipts bind exact contract files, complete declared journals, contract-required worker outputs, required review/approval evidence, budget usage, cross-team bindings, unresolved evidence, and terminal outcomes; any later append or artifact mutation must invalidate verification.

This is a review-first phase. Do not add tmux mutation, team-lead runtime, hierarchical transport, root scheduling, arbitrary recursion, a daemon, or multi-host behavior. Re-run all focused authority invariants and installed-wheel journeys, attack malformed/mixed identities and mutable or missing evidence, validate the prompt pack, audit release assets, and verify direct orchestration remains unchanged. Correct only demonstrated HIER-001/HIER-002 defects within the existing authority boundary; otherwise issue an evidence-backed HIER-GATE-0 accept or reject decision.
