# Delegation runbook

Use `docs/references/DELEGATION_RUNBOOK.md`. Run implementation tickets in
isolated worktrees. Parallel work is allowed only where the task manifest omits
a dependency. Child agents must not edit `docs/BACKLOG.md` or authorize their
own permissions, models, tools, acceptance criteria, or remediation policy.
