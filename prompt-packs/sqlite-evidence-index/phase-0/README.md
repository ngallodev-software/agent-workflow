# Phase 0 — authority schema and reconciliation

Implement and independently verify the authority boundary, versioned schema, deterministic rebuild/incremental reconciliation, provenance, and typed projections.

Execute `task-manifest.yaml` in dependency order. Implementation lanes with no dependency edge may run in isolated worktrees. The gate is review-only and must not implement missing scope.
