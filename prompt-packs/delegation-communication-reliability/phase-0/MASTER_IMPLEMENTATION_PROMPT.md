# Phase 0 — control-plane foundations

Read the current source, `docs/BACKLOG.md`, the pack README, the execution
protocol, every phase-0 ticket, and the referenced current implementations
before editing. Coordinate only `PROC-001` through `PROC-004`.

The goal is deterministic communication and evidence, not another messaging
broker or scheduler. Keep source journals and immutable launch/receipt
authority authoritative. Require installed-product journeys first, compact
invariant matrices only where journeys cannot cover the boundary, and truthful
completion evidence. Stop on ownership collision, scope expansion, missing
authority, or a proposed fallback that treats terminal text as delivery proof.
