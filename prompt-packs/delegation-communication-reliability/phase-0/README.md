# Phase 0 — control-plane foundations

Run `PROC-001`, `PROC-002`, `PROC-003`, and `PROC-004` concurrently in separate
worktrees. They have no dependency edges, but integration is serialized after
all handoffs are independently inspected.
