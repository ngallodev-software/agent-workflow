# Phase 2 — independent isolation gate

## Purpose

Integrate and attack the sandbox and redaction/retention boundaries.

## Tickets

- `ISO-GATE-01`: depends on `HARD-003`, `HARD-006`.

## Parallelism

No parallel implementation; use an independent reviewer.

Each ticket uses a separate worktree/session. Merge only after ticket evidence is reviewed, then rerun shared acceptance journeys before the next phase.
