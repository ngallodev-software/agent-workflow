schema: agent-workflow/pack/v1
pack_id: "codex-luna-effort-policy"
backlog_items:
  - "POL-001"
workflow:
  name: agent-workflow
  minimum_version: "0.3.0"
  terminal_backend: tmux
  requires:
    - isolated_worktree
    - foregroundable_session
    - persistent_log
    - source_baseline
    - completion_report
    - independent_phase_gate
    - release_drift_audit
