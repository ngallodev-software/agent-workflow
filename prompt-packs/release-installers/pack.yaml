schema: agent-workflow/pack/v1
pack_id: "release-installers"
backlog_items:
  - "REL-008"
workflow:
  name: agent-workflow
  minimum_version: "0.1.4"
  terminal_backend: tmux
  requires:
    - isolated_worktree
    - foregroundable_session
    - persistent_log
    - source_baseline
    - completion_report
    - independent_phase_gate
    - installed_product_acceptance
