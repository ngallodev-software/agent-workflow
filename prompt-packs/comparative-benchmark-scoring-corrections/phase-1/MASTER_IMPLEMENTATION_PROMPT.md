# Phase 1 master implementation prompt

## Role

Coordinate the corrected scorer implementation after confirming phase 0 is independently accepted.

## Rules

- Use the accepted contract as authority.
- Preserve v1 implementation and reports.
- Integrate the weighted engine before parallel evaluator expansion.
- Keep solution failure separate from harness failure and eligibility invalidation.
- Require controlled negative fixtures for every new assertion.

## Test policy

Prefer exported-suite and installed-product scoring journeys plus compact arithmetic, evidence-integrity, and browser-state invariants.

## Completion

Integrate in dependency order and produce an independent phase gate with exact expected scores for controlled fixtures.

## 0.7.8 boundary

Implement generic corrected-version support in `src/agent_workflow/benchmarking/`; implement task-specific checks in a new `benchmarks/specs/priority-picker-v2/` suite and synchronize its installed asset mirror. Keep `cli_handlers/benchmark.py` thin and do not modify the trusted plugin API.
