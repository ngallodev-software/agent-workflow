# Phase 0 master prompt

Execute only the inventory ticket in `task-manifest.yaml`. Use the indexed
Herdr and agent-workflow graphs first, preserve both repository baselines, and
stop on overlapping uncommitted changes. The gate independently reviews the
inventory and owns no implementation scope.
