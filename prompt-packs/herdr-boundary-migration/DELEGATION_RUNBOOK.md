# Delegation runbook

Use the repository `docs/references/DELEGATION_RUNBOOK.md` as the operational
authority. Run each ticket in an isolated worktree, index that exact worktree
when the optional service is available, preserve source baselines, collect a
schema-valid completion report, and require an independent phase gate.
