# Phase 4 — optional embedded sidebar

This phase is blocked even when dependencies are technically complete until the maintainer explicitly authorizes TMUXUI-008 in the canonical backlog. The prompt is retained for sequencing, not implicit approval.
