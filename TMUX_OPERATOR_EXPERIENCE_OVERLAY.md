# agent-workflow tmux Operator Experience Planning Overlay

This archive contains only new or changed repository-relative files for the tmux operator-experience recommendation and implementation prompt pack.

## Contents

- original comparative analysis;
- expanded architecture/product recommendation;
- canonical `docs/BACKLOG.md` additions plus detailed execution sequence;
- `docs/PROMPT_PACKS.md` active-pack registration;
- complete `prompt-packs/tmux-operator-experience/` with six phases, nine implementation backlog items, and two independent gates.

## Apply

Extract at the root of the matching `agent-workflow` source tree and review the diff before committing. The pack is intentionally dependency-gated on accepted `PROC-006` evidence. The optional embedded sidebar remains `needs-decision`.

## Important

This is a planning/prompt-pack overlay, not an implementation of the tmux UI. It does not include or copy source code from the reviewed prior-art repository.
