# Client hook reminders

These executable scripts are installed into the user data directory by
`install.sh` and registered as `SessionStart` hooks for Codex and Claude Code.
They are reminders, not authority boundaries. Agent-workflow receipts, RTK's
command rewriter, and codebase-memory's discovery gate remain the enforcement
surfaces where those clients support them.
