# Current Blockers and Resolution Plan

**Captured:** 2026-07-26
**Revision assessed:** `dfb0db1` plus the integrated HARD-004 authority implementation

This records the blockers reported after the previous release-check attempt and
the durable prerequisites for completing two-way orchestrator messaging.

## Blockers from the previous assessment

| Blocker | Evidence | Resolution state |
|---|---|---|
| `scripts/release-check.sh` could not import `pytest` | The script intentionally sets `PYTHONNOUSERSITE=1`; the selected system interpreter had no development dependencies. | **Actionable:** provide one repository `.venv`, install the `dev` extra there, and make release checks select or clearly require that interpreter. |
| Default suite had 11 executor/integration failures | `51 passed, 11 failed, 2 skipped, 5 xfailed`; the failures timed out while status remained `running` even though the fake executor had exited. | **Actionable:** make non-interactive launches use the non-interactive runner path and add a regression journey. |
| Two-way messaging was not complete | `BKL-001`/`BKL-002` and `MSG-001`/`MSG-002` remain unimplemented or only partially represented by current journal/watch/ack behavior. | **In progress:** implement durable cursor/inbox/supervisor behavior with installed-product evidence. |
| HARD-004 authority candidate is not yet acceptable | The delegated candidates stalled without valid completion contracts. The coordinator repaired and integrated the implementation; the installed acceptance slice and full release gate now pass. | **Resolved for implementation; in-review:** obtain the shared foundation gate and independent sealed disposition before marking accepted. |

## Durable prerequisites that remain real

The messaging prompt pack correctly identifies these prerequisites; they are
not cleared by documentation or by the existing best-effort `tmux` wake hint:

- `DEC-001` is now decided: local append-only JSONL authority, per-consumer
  FIFO, at-least-once append, digest-bound idempotency, rebuildable cursors,
  and a 2-second normal replay objective. See the [decision record](DECISIONS/DEC-001-DURABLE-CONTROL.md).
- `HARD-002`: **resolved for the individual prerequisite** by the accepted lifecycle receipt; filesystem-socket coverage remains a documented host limitation.
- `HARD-004`: the immutable launch/receipt authority implementation and installed acceptance are resolved. The item remains `in-review` until the shared foundation gate and independent sealed disposition are recorded.
- Later messaging lanes additionally depend on bounded execution,
  content-safety, authenticated identity, and executable trust controls.

## Resolution order

1. Bootstrap and document the single `.venv` used for development, builds,
   release checks, and tests. Keep `pytest` and `build` in the `dev` extra.
2. Implement the phase-0 durable cursor and orchestrator-inbox contracts in
   isolated worktrees after the accepted authority prerequisite.
3. Implement the shared supervisor/wakeup and executor-specific late-steering
   adapters only where delivery can produce correlated evidence.
4. Run the release-drift audit, installed-product suite, and sealed phase gate;
   update `docs/BACKLOG.md` only when the required evidence is present.

## Non-blocking but still open release work

License choice, a monitored vulnerability channel, clean-host compatibility,
release ownership/signing, and full provider/TMUX compatibility remain release
governance or environment evidence items. They are tracked in
[`RELEASE_BLOCKERS_AUDIT.md`](RELEASE_BLOCKERS_AUDIT.md) and are not silently
closed by the messaging implementation.
