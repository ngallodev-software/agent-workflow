# HARD-004 review and closure — 2026-07-28

## Decision

HARD-004 is accepted individually at commit `ef6393e` (`Fix HARD-004 restart
authority tamper boundary`). The implementation and its installed-product
regression coverage satisfy the ticket's immutable launch-authority,
projection-repair, receipt-digest, and status-tamper requirements.

This closes HARD-004 only. `HARD-005` remains in review,
`FOUND-GATE-01` remains rejected/open, and `MSG-001` remains blocked by the
shared foundation gate and its other declared prerequisites.

## Reviewed scope

The integrated change is the three-file repair on top of the existing
authority implementation:

- `src/agent_workflow/sessions.py`
- `schemas/command.schema.json`
- `tests/acceptance/test_delegation_journeys.py`

The review used the clean dedicated worktree
`/home/nate/.local/share/agent-workflow/worktrees/hard-004-review-fix-r1-20260728`
and the independent review worktree at the same source revision. No review
worktree changes were accepted.

## Structural evidence

The codebase-memory project `lump-apps-agent-workflow` was ready and refreshed
at review time with 6,906 nodes and 12,295 edges. Graph-first inspection found
and examined:

- `sessions.restart`: reads the immutable launch contract and append-only
  lifecycle state, verifies command fields/digest and prompt digest, and does
  not use mutable status selectors.
- `sessions._write_launch_contract`: writes launch-contract v2 atomically with
  mode `0444`, binding session, worktree, prompt, command, schemas, runtime,
  evaluation, source baseline, and expected outputs.
- `runner._authoritative_projection`: reconstructs launch-bound identity and
  paths while removing mutable receipt/disposition selectors.
- `state.repair_status`: rebuilds status from the launch contract and sealed
  receipt; pre-contract runs fail closed rather than inventing authority.
- `receipts.verify_seal_details`: returns the digest of the exact verified
  receipt bytes.

## Acceptance evidence

| Check | Result |
| --- | --- |
| Installed restart/status-tamper and authority/invariant slice | 26 passed |
| Full project suite | 103 passed, 2 skipped, 5 strict xfailed |
| Release asset audit | pass — `release assets: valid` |
| Active deterministic-foundations pack validation | pass — 3 phases, 5 tasks |
| Source revision | `ef6393eeb1db35974a0f62aafc37b7c23b9c08fa` |
| Evaluation plan | not required by the HARD-004 pack/ledger row |

The strict future HARD-004 test remains a planning placeholder for a broader
phase-gate scenario. The installed acceptance test
`test_restart_ignores_tampered_status_projection` is the exercised acceptance
journey for this implementation.

The repository release script reaches the same full test result but exits
afterward on the host-only setting `terminal.agent_stack` in
`/home/nate/.config/agent-workflow/config.toml`. That user configuration was
not changed as part of this ticket; it is a release-environment cleanup item,
not a HARD-004 implementation failure.

## Delegation evidence and limitation

The implementation repair was completed in an interactive dedicated worktree
and integrated at `ef6393e`. Three separate non-interactive review attempts
were launched and closed through agent-workflow:

- `hard-004-independent-review-r1-20260728` stopped on a Codex provider model
  metadata/schema error before producing a handoff.
- `hard-004-independent-review-r2-20260728` was blocked by the Claude
  discovery hook/MCP permission boundary and produced no valid completion
  handoff.
- `hard-004-independent-review-r3-20260728` indexed and inspected the graph,
  then stalled during command/source review and was closed with missing
  completion validation.

Their sealed termination receipts are retained in the local run store, but
none is treated as an accepted reviewer disposition. The closure decision in
this document is the coordinator's independent, source-and-test review; no
missing delegated receipt is being represented as successful review evidence.

All three review panes/sessions were closed through agent-workflow and verified
absent from tmux. The unrelated active PROC-001 session was left untouched.

## Commands

```text
rtk .venv/bin/python -m pytest -q tests/acceptance/test_delegation_journeys.py::test_executor_failure_is_terminal_sealed_and_restartable tests/acceptance/test_delegation_journeys.py::test_restart_ignores_tampered_status_projection tests/invariants/test_security_boundaries.py tests/invariants/test_durable_state.py tests/invariants/test_sealed_run_assessment.py tests/invariants/test_hard_008_config_trust.py
  26 passed

rtk .venv/bin/python -m pytest -q
  103 passed, 2 skipped, 5 xfailed

rtk .venv/bin/python scripts/audit-release-assets.py
  release assets: valid

rtk agent-workflow pack validate prompt-packs/deterministic-enforcement-foundations
  valid: True
```

