# HARD-005 review and closure — 2026-07-28

## Decision

HARD-005 is accepted individually against the current integrated tree. The
implementation landed in `8fde4c3` and remains present at `5f2b9dc`; the
current review verified the MCP read boundary with the installed stdio client.

This closes the HARD-005 ticket. It does not authorize MCP-003 mutation work,
which still requires HARD-007.

## Evidence

The codebase-memory project `lump-apps-agent-workflow` was ready at review time
with 6,916 nodes and 12,301 edges. Graph-first inspection covered the MCP
message metadata, receipt, path-validation, command-context, and stdio service
symbols before source/test inspection.

| Check | Result |
| --- | --- |
| Installed stdio MCP journey | 7 passed |
| Security, durable-state, and sealed-assessment slices | 18 passed |
| HARD-008 trust/config acceptance slices | 14 passed |
| Full project suite | 103 passed, 2 skipped, 5 strict xfailed |
| Release asset audit | pass — `release assets: valid` |
| Active pack validations | all four required packs valid |
| Reviewed revision | `5f2b9dc` |

The installed stdio journey verified bounded metadata-only responses, synthetic
secret redaction, no raw repository paths, command-context/card digest parity,
and fail-closed tampering. The invariant matrices cover no-follow path
replacement, receipt integrity, pagination, and stable error boundaries.

## Remaining limitations

The original implementation run remains a `partial` completion because its
host could not run the optional installed MCP fixture and could not write
progress to the host state root. The current project `.venv` supplied the
missing MCP dependency and independently reproduced the installed journey.
No delegated completion receipt is being relabeled as accepted; this is a
coordinator review of the integrated source and current acceptance evidence.

