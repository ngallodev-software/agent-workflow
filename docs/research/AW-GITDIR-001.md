# AW-GITDIR-001

Fix headless Agent Run launch for linked Git worktrees: include the resolved Git
administrative directory in Codex's writable scope so workers can stage and
commit. Add a focused regression test, update the backlog, run relevant tests,
commit, and publish structured completion.
